// Central runtime config for the web app.
// For Phase 0 we hardcode the dev API origin; later swap to Angular
// environment file replacements / build-time injection.
export const API_BASE_URL = 'http://localhost:3000';
